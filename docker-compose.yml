version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:80"
    volumes:
      - .:/app
    restart: unless-stopped
    networks:
      - devopsflow-network

networks:
  devopsflow-network:
    driver: bridge 